# paper/tas/ — the TAS submission record only

The one live paper is **`papers/marginally-useful/marginally-useful.tex`**, seven pages.

What is here is the record of what The American Statistician actually has, frozen.
`submitted-2026-06-11/` holds the exact submission, blind and non-blind PDFs plus source.
`marginallyuseful.pdf` and `marginallyusefulanon.pdf` are those same two files.
Do not edit any of them.

The 26-page revision of 2026-08-20 is not on disk. It is in git at **`9215785`**:

    git show 9215785:paper/tas/marginally-useful.tex > /tmp/long.tex

That version is the only place the following now lives, all dropped from the short paper
on purpose: three of the five readings of the identity (log Bayes factor, conditional
non-uniformity of conformal ranks, information projection), the exchangeability and
time-series section, the coverage-as-objective section, and five figures.

The old TMLR and arXiv submission packs were also deleted from the working tree on
2026-08-20 to leave one paper on disk. They remain in git history.
